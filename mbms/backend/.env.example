DATABASE_URL="postgresql://mbms:mbms_dev_password@localhost:5442/mbms_identity_org?schema=public"

JWT_ACCESS_SECRET="change-me"
JWT_ACCESS_EXPIRES_IN=900s
JWT_REFRESH_EXPIRES_IN_DAYS=7

JWT_CUSTOMER_ACCESS_SECRET="change-me"
JWT_CUSTOMER_ACCESS_EXPIRES_IN=900s
JWT_CUSTOMER_REFRESH_EXPIRES_IN_DAYS=7

# FR-AUTH-04: configurable password policy minimum length (complexity —
# uppercase/lowercase/digit — is fixed, not configurable).
PASSWORD_MIN_LENGTH=8

PORT=3001
