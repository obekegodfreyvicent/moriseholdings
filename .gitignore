# Dependencies
node_modules/

# Build output / caches
dist/
build/
*.tsbuildinfo
.vite/

# Environment / secrets (keep .env.example and .env.ssl.example)
.env
.env.*
!.env.example
!.env.ssl.example

# Let's Encrypt certificates + ACME webroot issued locally by
# mbms/deploy/init-letsencrypt.sh — private keys, never committed.
mbms/deploy/certbot/

# docx_backup*/ snapshot directories (root + nested under docx/).
# Git history is the version trail; these local safety copies are not tracked.
docx_backup*/

# LibreOffice / editor lock + temp files
.~lock.*#
*~

# OS cruft
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Local Netlify folder
.netlify
